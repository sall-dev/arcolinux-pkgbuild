post_install() {
    echo -e "\n** The files have been installed in /etc/skel/.xmonad **"
    echo -e "** The bar needs the following applications **"
    echo -e "** - checkupdates-aur **"
    echo -e "** - gsimplecal **\n"
}

post_upgrade() {
    post_install
}